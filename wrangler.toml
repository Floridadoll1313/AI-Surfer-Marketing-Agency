name = "ocean-tide-drop"
compatibility_date = "2024-12-01"

# Required for Cloudflare Pages (v2 root directory strategy)
pages_build_output_dir = "dist"

# Optional but recommended for Workers compatibility
main = "dist/_worker.js"