{
  "name": "createLore",
  "description": "Generate mythic surf-world lore for characters, locations, or artifacts.",
  "input_schema": {
    "type": "object",
    "properties": {
      "subject": { "type": "string" },
      "intensity": { "type": "number", "minimum": 1, "maximum": 10 }
    },
    "required": ["subject"]
  }
}